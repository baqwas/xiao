name: XIAO_S3_Final_Clean_Build
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Arduino CLI
        uses: arduino/setup-arduino-cli@v2

      - name: Install ESP32 Platform
        run: |
          # 1. Clear any potential cached data in the runner
          rm -rf ~/.arduino15

          # 2. Re-initialize config from scratch
          arduino-cli config init --overwrite
          arduino-cli config add board_manager.additional_urls https://files.seeedstudio.com/arduino/package_seeeduino_boards_index.json

          # 3. Update index and install the core
          arduino-cli core update-index
          arduino-cli core install esp32:esp32

      - name: Compile Sketch
        run: |
          # Use the verified FQBN
          arduino-cli compile --fqbn esp32:esp32:xiao_esp32s3 ./ --verbose
